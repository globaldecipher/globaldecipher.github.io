{
  "name": "tgd-incidents-worker",
  "version": "1.0.0",
  "private": true,
  "description": "TGD Cloudflare Worker: official X-to-Map agent, D1 editorial API, KV map feed, R2 exports, and analytics.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.118.0"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
