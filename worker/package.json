{
  "name": "tgd-incidents-worker",
  "version": "1.0.0",
  "private": true,
  "description": "TGD Cloudflare Worker: D1 editorial API, KV incident feed, R2 media, analytics, and scheduled report drafts.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test test/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
